"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const yargs_1 = __importDefault(require("yargs"));
const Server_1 = __importDefault(require("./Server"));
const TestTool_1 = __importDefault(require("./TestTool"));
// const argv = yargs.options({
//   port: { type: 'number' },
// }).argv
yargs_1.default
    .command('server [port]', 'start the server', (yargs) => {
    yargs
        .positional('port', {
        describe: 'port to bind on',
        default: 3000,
    });
}, (argv) => {
    if (argv.verbose)
        console.info(`start server on :${argv.port}`);
    const port = argv.port;
    new Server_1.default({ port: port });
})
    .command('build', 'build your static website', (yargs) => { }, (argv) => {
    console.error('not yet implemented');
})
    .command('test', 'test if your startpage is compatible with https://customstart.page', (yargs) => { }, (argv) => {
    new TestTool_1.default()
        .run();
})
    .option('verbose', {
    alias: 'v',
    type: 'boolean',
    description: 'Run with verbose logging'
})
    .demandCommand()
    .help()
    .argv;
//# sourceMappingURL=index.js.map