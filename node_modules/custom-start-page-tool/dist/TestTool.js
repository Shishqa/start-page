"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = __importDefault(require("fs"));
class TestTool {
    constructor() {
    }
    run() {
        return this._checkRequiredFiles();
    }
    _anyPathExists(...paths) {
        let exists = false;
        for (const path of paths) {
            exists = fs_1.default.existsSync(path);
            if (exists)
                break;
        }
        // if (exists == false) {
        //     console.error(`❌ ${paths.join(' or ')} not found.`)
        // } else {
        //     console.log(`✅ ${paths.join(' or ')} found.`)
        // }
        return {
            paths: paths,
            exists: exists,
        };
    }
    _checkRequiredFiles() {
        let testResults = {
            results: [],
        };
        testResults.results.push(this._anyPathExists('./index.ejs', './index.html'));
        testResults.results.push(this._anyPathExists('./manifest/defaultData.json'));
        testResults.results.push(this._anyPathExists('./manifest/meta.json'));
        testResults.results.push(this._anyPathExists('./manifest/preview.png', './manifest/preview.jpg'));
        testResults.results.push(this._anyPathExists('./manifest/readme.md'));
        testResults.results.push(this._anyPathExists('./manifest/schema.json'));
        // if (errorFound) {
        //     console.error('❌ Test failed.')
        // } else {
        //     console.log('✅ Test passed.')
        // }
        return testResults;
    }
}
exports.default = TestTool;
//# sourceMappingURL=TestTool.js.map