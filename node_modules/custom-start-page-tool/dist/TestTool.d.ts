export interface TestResult {
    paths: string[];
    exists: boolean;
}
export interface TestResults {
    results: TestResult[];
}
export default class TestTool {
    constructor();
    run(): TestResults;
    _anyPathExists(...paths: string[]): TestResult;
    private _checkRequiredFiles;
}
